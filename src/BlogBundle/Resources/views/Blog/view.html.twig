{% extends "BlogBundle::layout.html.twig" %}
{% block title %}
    {{ blog.title }}
{% endblock %}

{% block content %}
    <h1>{{ blog.title }}</h1>
    {{ blog.body|raw }}
    {{ blog.created|date("d-m-Y") }}
{% endblock %}
